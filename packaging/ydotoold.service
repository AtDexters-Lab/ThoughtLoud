[Unit]
Description=ThoughtLoud keyboard input daemon
After=default.target

[Service]
ExecStart=/opt/voxd/libexec/ydotoold --socket-path=%h/.voxd_ydotool_socket --socket-own=%U:%G
Restart=on-failure
RestartSec=1s

[Install]
WantedBy=default.target
